# Copy this file to .env and fill in real values. Never commit .env itself.

# Gemini, used for listing extraction (phase 1), eligibility interpretation (phase 2),
# and trust reasoning (phase 3)
GOOGLE_API_KEY=

# Google Custom Search JSON API (phase 3 trust verification)
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_ID=
