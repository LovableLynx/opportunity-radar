{
  "name": "opportunity-radar-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "playwright test",
    "test:api": "node --test api/*.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "http-server": "^14.1.1"
  }
}
